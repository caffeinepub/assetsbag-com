{
  "kind": "implementation_plan",
  "version": "1.0",
  "title": "Step 2 (Frontend-only): Align frontend actor typing + verification docs for access-control init method",
  "requirements": [
    {
      "id": "REQ-9",
      "summary": "Ensure the frontend remains compatible with the backend initialization API by keeping the immutable initialization call intact while aligning generated frontend actor typings and documenting the required backend public method contract.",
      "acceptanceCriteria": [
        "Backend deploy succeeds (no init-time failure).",
        "Frontend actor initialization no longer throws due to missing `_initializeAccessControlWithSecret`.",
        "No changes are made to immutable frontend paths (including `frontend/src/hooks/useActor.ts`)."
      ],
      "file_operations": [
        {
          "path": "frontend/src/backend.d.ts",
          "operation": "modify",
          "description": "Update the frontend backend-interface TypeScript declarations to include the initialization method required by the immutable call site: `actor._initializeAccessControlWithSecret(adminToken) : Promise<void>`. Keep the signature consistent with the backend’s public API after the backend-side fix so TypeScript matches runtime behavior."
        },
        {
          "path": "frontend/docs/deploy-failure-root-cause.md",
          "operation": "modify",
          "description": "Update the analysis and verification guidance to reflect the required compatibility contract: the backend must publicly expose a method matching the immutable frontend call `actor._initializeAccessControlWithSecret(adminToken)`. Include a checklist item to confirm the method appears in the generated frontend declarations and no runtime error occurs during actor initialization."
        },
        {
          "path": "frontend/docs/rebuild-checklist.md",
          "operation": "modify",
          "description": "Complete/expand the rebuild checklist to include the mandatory regeneration and verification steps after backend interface changes, specifically checking that `_initializeAccessControlWithSecret` is present in the generated frontend declarations and that the authenticated actor initialization path runs without errors."
        }
      ]
    }
  ]
}